import 'package:flutter/material.dart';

Widget mobileImageWidget(String path) {
  // Tidak dipakai di Web, bisa lempar error atau placeholder
  return const Center(child: Text('Tidak bisa tampilkan gambar di Web'));
}
